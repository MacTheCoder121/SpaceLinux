# Boot SpaceLinux
Make sure the following packages: grub-pc-bin and qemu-system-x86.
<br>
You can just run startup.sh.
# Boot SpaceLinux With Source Code
Make sure the packages mtools,ld,gcc,grub-pc-bin,qemu-system-x86,and the GAS compiler.
<br>
You can always edit grub and kernel.c, but others are NOT recommended. You can copy this
<br>
just make sure you give credit. Run compile.sh, and it will run it automatically,
<br>
when you shut the VM down, you can run startup.sh
